"""Backward-compatibility stub — runner scripts moved to sandbox_runners/.

The actual runner scripts are now real Python files in
``cnake_charmer/eval/sandbox_runners/`` so they can be linted, tested,
and ro-bound into the bwrap sandbox. See that package's docstring for
sandbox constraints.
"""
