[
  {
    "type": "function",
    "function": {
      "name": "evaluate_cython",
      "description": "Compile, analyze, test, and benchmark Cython code in one step. Returns compilation status, annotation score with optimization hints, correctness test results, and speedup measurement. If compilation fails, only error messages are returned. Fix any issues and call again. Set annotate/test/benchmark to False to skip expensive steps.",
      "parameters": {
        "type": "object",
        "properties": {
          "code": {
            "type": "string",
            "description": "Complete .pyx source code."
          },
          "annotate": {
            "type": "boolean",
            "description": "Run annotation analysis (default true).",
            "default": true
          },
          "test": {
            "type": "boolean",
            "description": "Run correctness tests (default true).",
            "default": true
          },
          "benchmark": {
            "type": "boolean",
            "description": "Measure speedup (default true).",
            "default": true
          }
        },
        "required": ["code"]
      }
    }
  }
]
